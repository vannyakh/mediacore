package io.mediacore;

/**
 * Future MediaCore Java SDK stub.
 *
 * <pre>
 * client.media().analyze(url);
 * client.media().download(url, format);
 * client.media().convert(path, options);
 * client.media().thumbnail(url);
 * client.jobs().list();
 * client.plugins().list();
 * </pre>
 */
public final class MediaCore {
    private MediaCore() {}

    public static void main(String[] args) {
        throw new UnsupportedOperationException(
            "MediaCore Java SDK is planned. See docs/sdk.md for the unified API surface.");
    }
}
