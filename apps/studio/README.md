# MediaCore Studio

Visual workflow builder (React Flow / Node-RED style).

```text
URL → Analyze → Download → Convert → Subtitle → Translate → Thumbnail → Export
```

Scaffold only in v0.1 — full editor targeted for roadmap v0.4.
