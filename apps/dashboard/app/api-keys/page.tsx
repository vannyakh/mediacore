export default function ApiKeysPage() {
  return (
    <section>
      <h1>API Keys</h1>
      <p className="lead">Manage API keys used by clients and SDKs.</p>
      <div className="panel">
        <p className="muted">Dev seed key is configured via <code>SEED_API_KEY</code>.</p>
      </div>
    </section>
  );
}
