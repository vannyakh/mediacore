export default function SettingsPage() {
  return (
    <section>
      <h1>Settings</h1>
      <p className="lead">Environment and platform configuration.</p>
      <div className="panel">
        <p className="muted">Configure via environment variables / .env — see project .env.example.</p>
      </div>
    </section>
  );
}
